# Приложение для анализа транзакций
## Описание:
 приложение для анализа транзакций, которые находятся в Excel-файле. Приложение будет генерировать JSON-данные для веб-страниц, формировать Excel-отчеты, а также предоставлять другие сервисы.
## Установка:

1. Клонируйте репозиторий:
```shell
https://github.com/NikToo-13/Analysis_bank_operations
```
2. Установка зависимостей:
```shell
pip install -r requirements.txt
```
## Использование:
Функции вызываются по мере необходимости из пакетов utils.py, лежащих в папки src.


## Модуль: utils.py
Содержит воспомогательные функции.


read_excels(str) - Функция принимает путь к файлу в виде строки. И возвращает объект: DataFrame

*пример:*
```shell
print(type(read_excels('C:\user\operation.xlsx')))
```
*результат* -> class 'pandas.core.frame.DataFrame'


convert_dataf_listd(DataFrame) - Функция конвертирует DataFrame в список словарей.

*пример:*
```shell
print(convert_dataf_listd(DataFrame))
```
*результат* -> [{'Дата операции': '01.01.2018 12:49:53', 'Дата платежа': '01.01.2018', 'Номер карты': 'nan', 
'Статус': 'OK', 'Сумма операции': '-3000.0', 'Валюта операции': 'RUB', 'Сумма платежа': '-3000.0', 
'Валюта платежа': 'RUB', 'Кэшбэк': 'nan', 'Категория': 'Переводы', 'MCC': 'nan', 'Описание': 'Линзомат ТЦ Юность', 
'Бонусы (включая кэшбэк)': '0', 'Округление на инвесткопилку': '0', 'Сумма операции с округлением': '3000.0'}]


operation_filter(str):list - Функция принимает на вход строку с датой и временем в формате
YYYY-MM-DD HH:MM:SS  и возвращает данные в виде списка словарей с начала месяца, на который
выпадает входящая дата, по входящую дату.

```shell
operation_filter('2021-12-30 15:45:00')
```
*результат* -> [{'Дата операции': '30.12.2021 17:50:17', 'Дата платежа': '30.12.2021', 'Номер карты': '*4556', 'Статус': 'OK', 'Сумма операции': 174000.0, 'Валюта операции': 'RUB', 'Сумма платежа': 174000.0, 'Валюта платежа': 'RUB', 'Кэшбэк': nan, 'Категория': 'Пополнения', 'MCC': nan, 'Описание': 'Пополнение через Газпромбанк', 'Бонусы (включая кэшбэк)': 0, 'Округление на инвесткопилку': 0, 'Сумма операции с округлением': 174000.0}
{'Дата операции': '30.12.2021 14:48:25', 'Дата платежа': '30.12.2021', 'Номер карты': '*7197', 'Статус': 'OK', 'Сумма операции': -349.0, 'Валюта операции': 'RUB', 'Сумма платежа': -349.0, 'Валюта платежа': 'RUB', 'Кэшбэк': nan, 'Категория': 'Канцтовары', 'MCC': 5943.0, 'Описание': 'Mitrankov M.V.', 'Бонусы (включая кэшбэк)': 6, 'Округление на инвесткопилку': 0, 'Сумма операции с округлением': 349.0}]


user_settings_read(str):json - Функция чтения файла пользовательских на строек. Принимает имя файла, по умолчанию файл:user_settings.json и возвращает файл JSON 

```shell
user_settings_read()
```
*результат* -> {'user_currencies': ['USD', 'EUR'], 'user_stocks': ['AAPL', 'AMZN', 'GOOGL', 'MSFT', 'TSLA']}

## Модуль: views.py
Основные функции для генерации JSON-ответов

greetings() - Функция приветствия в зависимости от времени суток «Доброе утро» / «Добрый день» / «Добрый вечер» / «Доброй ночи»
```shell
greetings()
```
*результат*  -> [{"greeting":Доброй ночи}]

for_each_card(list, b:float):list - Функция возвращающая список словорей по каждой карте(последние 4 цифры карты; общая сумма расходов;
          кешбэк (1 рубль на каждые установленое значение в переменной b ))
```shell
for_each_card(transactions)
```
*результат* -> [{last_digits': '7197', 'total_spent': 1033.73, 'cashback': 10.34}, {'last_digits': '5091', 'total_spent': 5522.2, 'cashback': 55.22}]


top_transactions_amount(list):list - Функция выводит Топ-5 транзакций по сумме платежа.

```shell
top_transactions_amount(transactions)
```
*результат* ->[{'date': '30.12.2021 17:50:17', 'amount': 174000.0, 'category': 'Пополнения', 'discription': 'Пополнение через Газпромбанк'}, 
               {'date': '22.12.2021 23:30:45', 'amount': 28001.94, 'category': 'Переводы', 'discription': 'Перевод Кредитная карта. ТП 10.2 RUR'}, 
               {'date': '23.12.2021 16:45:12', 'amount': 20000.0, 'category': 'Другое', 'discription': 'Иван С.'}, 
               {'date': '30.12.2021 17:50:30', 'amount': 5046.0, 'category': 'Пополнения', 'discription': 'Пополнение через Газпромбанк'}, 
               {'date': '05.12.2021 15:20:00', 'amount': 3500.0, 'category': 'Пополнения', 'discription': 'Внесение наличных через банкомат Тинькофф'}]

list_currencies_user(list):dict - Функция возвращает словарь с курсами валют, на основе пользовательских настроек через запрос API

```shell
list_currencies_user(currencies_user)
```
*результат* -> [{'currency': 'USD', 'rate': '78.49'}, 
                {'currency': 'EUR', 'rate': '90.14'}]

list_stock_user(clist):dict -> Функция возвращает словарь с курсами акций, на основе пользовательских настроек через запрос API

```shell
list_stock_user(currencies_user)
```
*результат* -> [{'stock': 'AAPL', 'price': '150.12'}, 
                {'stock': 'AMZN', 'price': '3173.18'}, 
                {'stock': 'GOOGL', 'price': '2742.39'}, 
                {'stock': 'MSFT', 'price': '296.71'}, 
                {'stock': 'TSLA', 'price': '1007.08'}]

views(str):json - функция принимает дату в формате Приветствие в формате:YYYY-MM-DD HH:MM:SS и возвращает
    приветствие в зависимости от текущего времени,
    По каждой карте: последние 4 цифры карты; общая сумма расходов; кешбэк (1 рубль на каждые 100 рублей).
    Топ-5 транзакций по сумме платежа.
    Курс валют.
    Стоимость акций из S&P500.

```shell
views('2021-12-30 15:45:00')
```
*результат* -> {
                'greeting': 'Доброй ночи', 
                'cards': [{'last_digits': '7197', 'total_spent': 24155.57, 'cashback': 241.56}, 
                          {'last_digits': '5091', 'total_spent': 14629.33, 'cashback': 146.29}, 
                          {'last_digits': '4556', 'total_spent': 3775.7, 'cashback': 37.76}], 
                'top_transactions': [{'last_digits': '7197', 'total_spent': 24155.57, 'cashback': 241.56}, 
                                     {'last_digits': '5091', 'total_spent': 14629.33, 'cashback': 146.29}, 
                                     {'last_digits': '4556', 'total_spent': 3775.7, 'cashback': 37.76}], 
                'currency_rates':  [{'currency': 'USD', 'rate': '78.49'}, 
                                    {'currency': 'EUR', 'rate': '90.14'}], 
                'stock_price': [{'stock': 'AAPL', 'price': '150.12'}, 
                                {'stock': 'AMZN', 'price': '3173.18'}, 
                                {'stock': 'GOOGL', 'price': '2742.39'}, 
                                {'stock': 'MSFT', 'price': '296.71'}, 
                                {'stock': 'TSLA', 'price': '1007.08'}]
                }

## Модуль: reports.py 
Модуль отчетов

spending_by_category(pd.DataFrame, str, Optional[str] = None): pd.DataFrame - Функция возвращает траты по заданной категории за последние три месяца (от переданной даты)

```shell
spending_by_category(date_frame, "Переводы", "2018-12-30 15:45:00")
```
*результат* -> 
            Дата операции  ... Сумма операции с округлением
5088  29.12.2018 23:10:23  ...                     70000.00
5102  25.12.2018 09:21:48  ...                      5000.00
5111  22.12.2018 21:52:33  ...                     10000.00
............

report(func) - Декоратор без входных значений записывает результат в файл report.xlsx

```shell
@report
def spending_by_category(pd.DataFrame, str, Optional[str] = None) -> pd.DataFrame:
```
*результат* -> данные записываются в файл report.xlsx, корневой дириктории report проекта

report_file(file) - Декоратор принимает имя файла в качестве параметра и записывает в него

```shell
@report_file(file)
def spending_by_category(pd.DataFrame, str, Optional[str] = None) -> pd.DataFrame:
```
*результат* -> данные записываются в заданный файл в переменной file, корневой дириктории report проекта


## Документация:

## Лицензия: 

Этот проект учебный